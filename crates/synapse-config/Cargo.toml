[package]
name = "synapse-config"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Config loading for the synapse rewrite daemon. See doc/REWRITE_ROADMAP.md."

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
thiserror = { workspace = true }
directories = "5"

[dev-dependencies]
tempfile = "3"
